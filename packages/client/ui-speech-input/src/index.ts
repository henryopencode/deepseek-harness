/**
 * Speech-input surface plugin, node half. The Host transcription service is a
 * separate Cordis row; this package's browser half contributes the recorder.
 */

/** Host plugin body — no Host-side behavior for this presentation plugin. */
export function apply(): void {}
